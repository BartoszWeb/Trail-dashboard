import styled from "styled-components";

export const NumberOfBoxPopUp = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  padding: 5px;
  background-color: ${ ({ theme }) => theme.colors.primaryColor };
  text-align: center;
`