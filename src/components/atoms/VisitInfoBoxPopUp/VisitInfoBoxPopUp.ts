import styled from "styled-components";

export const VisitInfoBoxPopUp = styled.div`
  position: absolute;
  bottom: 0;
  left: 0;
  font-size: 10px;
  padding: 2px;
  text-align: center;
  color: ${ ({ theme }) => theme.colors.textColor };
`