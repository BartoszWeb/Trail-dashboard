import styled from "styled-components";

export const TimeInfoBoxPopUp = styled.div`
  position: absolute;
  bottom: 0;
  right: 0;
  padding: 5px;
  background-color: ${ ({ theme }) => theme.colors.primaryColor };
  text-align: center;
`